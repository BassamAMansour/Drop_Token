package com.drop_token.controller;

public interface IDTController {

    /**
     * Starts the game main loop, manages asks for the input and print out the results.
     * Manages the logic of the gameplay, and communicates directly with the game engine.
     * */
     void startGame();
}
